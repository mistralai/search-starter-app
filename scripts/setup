#!/usr/bin/env bash
# Convenience wrapper around `copier copy` (no --trust required).
set -euo pipefail

TEMPLATE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
DEST="${1:?Usage: $0 <destination>}"
shift || true

exec copier copy "${TEMPLATE_DIR}" "${DEST}" "$@"
